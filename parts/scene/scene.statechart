BaseScene

Scene1* :: BaseScene
    on state.enter
        _: console.log("Entered scene 1!")

    on move_leg -> Scene2

Scene2 :: BaseScene
    on state.enter
        _: alert("Joe kicks open the door and stumbles into the pitch-dark room...")

    on move_arm -> Scene2
